import { sessionFeature } from './session.reducer';

export const {
  selectStatus,
  selectStart,
  selectEnd,
  selectIndex,
  selectSessionChars,
  selectKeystrokes,
  selectErrors,
} = sessionFeature;
